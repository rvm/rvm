#!/usr/bin/env bash

requirements_arch_lib_installed_all_missing()
{
  pacman -T "$@" >/dev/null || true
}

requirements_arch_libs_install()
{
  __rvm_try_sudo pacman -Sy --needed --recursive --noconfirm "$@" || return $?
}

requirements_arch_libs_ensure()
{
  typeset -a packages_installed packages_missing packages_to_install
  __rvm_filter_installed_packages arch "$@" || return $?
}

requirements_arch_run()
{
  case "$1" in
    (update-system)
      rvm_requiremnts_fail_or_run_action 3 \
        "Skipping pacman update, make sure your system is up to date." \
        requirements_arch_libs_install pacman || return $?
      ;;
    (rvm)
      requirements_arch_libs_ensure bash curl patch
      ;;
    (jruby*head)
      requirements_arch_libs_ensure jdk7-openjdk jre7-openjdk curl apache-ant git
      ;;
    (jruby*)
      requirements_arch_libs_ensure jdk7-openjdk jre7-openjdk curl
      ;;
    (ir*)
      requirements_arch_libs_ensure mono
      ;;
    (opal)
      requirements_arch_libs_ensure nodejs
      ;;
    (*-head)
      requirements_arch_libs_ensure gcc patch curl zlib readline libxml2 libxslt autoconf automake diffutils make libtool bison git
      ;;
    (*)
      requirements_arch_libs_ensure gcc patch curl zlib readline libxml2 libxslt autoconf automake diffutils make libtool bison
      ;;
  esac
}

requirements_manjaro_run()
{
  requirements_arch_run "$@" || return $?
}
