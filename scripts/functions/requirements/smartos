#!/usr/bin/env bash

requirements_smartos_run()
{
  case "$1" in
    (update-system)
      true
      ;;
    (rvm)
      __rvm_try_sudo pkgin bash curl git patch
      ;;
    (jruby-head*)
      __rvm_try_sudo pkgin jdk apache-ant
      ;;
    (jruby*)
      __rvm_try_sudo pkgin jdk
      ;;
    (ir*)
      __rvm_try_sudo pkgin mono
      ;;
    (opal)
      __rvm_try_sudo pkgin node-devel
      ;;
    (*)
      __rvm_try_sudo pkgin coreutils gcc47 gmake readline scmgit-base sqlite3 libxml2 libxslt ncurses autoconf automake
      ;;
  esac
}
