#!/bin/bash
set -x

mkdir -p ~/.bin/

if  [ ! -s ~/.bin/rvm ] ; then
  ln -fs `pwd`/bin/rvm ~/.bin/rvm
fi

grep "~/.bin" ~/.profile
if [ $? != 0 ] ; then
  echo "PATH=~/.bin:\$PATH" >> ~/.profile
fi

source ~/.profile

